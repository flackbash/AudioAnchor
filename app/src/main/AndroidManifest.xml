<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Required so the app can access images that other apps created, e.g. cover images -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Required so the app can access audio files that other apps created -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Some supported audio containers (.webm, .mkv, .3gp) are classified as video by
         MediaProvider, so without this permission scoped storage hides them from the app
         even though it never wants to see actual videos -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true">
        <activity
            android:name=".activities.DirectoryActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.prangesoftwaresolutions.audioanchor.activities.MainActivity" />
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".data.AnchorProvider"
            android:authorities="${applicationId}"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".activities.AlbumActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.prangesoftwaresolutions.audioanchor.activities.MainActivity" />
        </activity>
        <activity
            android:name=".activities.PlayActivity"
            android:clearTaskOnLaunch="true"
            android:launchMode="singleTask">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.prangesoftwaresolutions.audioanchor.activities.AlbumActivity" />
        </activity>
        <activity
            android:name=".activities.SettingsActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.prangesoftwaresolutions.audioanchor.activities.MainActivity" />
        </activity>
        <activity android:name=".activities.AboutActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.prangesoftwaresolutions.audioanchor.activities.MainActivity" />
        </activity>

        <service android:name=".services.MediaPlayerService"
            android:exported="false"
            android:enabled="true"
            android:foregroundServiceType="mediaPlayback"/>

        <receiver android:name=".receivers.MediaButtonIntentReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
